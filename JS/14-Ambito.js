'use strict'

function holaMundo(texto){
    console.log(texto);
    console.log(numero.toString());
    var hola_mundo = "texto de función" /**
    Las variables locales de una función no se pueden llamar por fuera de las funciones
    Mas sin embargo, las variables fuera de las funciones, si se pueden llamar dentro de la función
    */

}var numero = 12;
var texto="Hola mundo";
holaMundo(texto);